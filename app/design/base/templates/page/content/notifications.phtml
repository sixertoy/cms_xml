<?php
$messages = $this->getHelper()->getMessages();
if( !empty( $messages ) ) :
?>
<div class="alert alert-error">
	<h4>Erreurs!</h4>
	<button type="button" class="close" data-dismiss="alert">x</button>
	<ul>
		<?php foreach( $messages as $debugMessage ) : ?>
		<li><?php echo $debugMessage->getTextMessage(); ?></li>
		<?php endforeach; ?>
	</ul>
</div>
<?php endif; ?>